apiVersion: v1
kind: Pod
metadata:
  name: gpu-test
  labels:
    app: gpu-scheduling-lab
spec:
  restartPolicy: Never
  containers:
    - name: cuda-vector-add
      image: nvcr.io/nvidia/k8s/cuda-sample:vectoradd-cuda12.5.0
      resources:
        limits:
          nvidia.com/gpu: 1
